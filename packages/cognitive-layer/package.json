{
  "name": "@ois/cognitive-layer",
  "version": "0.1.0",
  "description": "Sovereign cognitive-middleware layer for OIS agents — pluggable chain-of-responsibility augments for tool-call interception, caching, dedup, description enrichment, error normalization, and telemetry. Consumed by @ois/network-adapter via opt-in config. See ADR-018.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}
