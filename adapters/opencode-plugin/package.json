{
  "name": "@apnex/opencode-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "OpenCode host bundle — stub-only at mission-64 W1+W2 (per anti-goal §4.2 #5); namespace migrated to @apnex; full parity in dedicated future mission. Not published to registry this mission.",
  "license": "MIT",
  "type": "module",
  "main": "src/shim.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@apnex/cognitive-layer": "*",
    "@apnex/message-router": "*",
    "@apnex/network-adapter": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^0.4.30",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}
