# Terraform state (local backend — do not commit)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Environment files with secrets
env/*.tfvars
!env/*.tfvars.example

# Archive directory (pre-split monolith snapshot — reference only, not reinstalled)
archive-pre-split-*/
